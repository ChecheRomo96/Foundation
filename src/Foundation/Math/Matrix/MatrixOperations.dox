/**
 * @defgroup Foundation_MatrixOperations Matrix Operations
 * @ingroup Foundation_Matrix
 *
 * @brief Matrix arithmetic algorithms.
 *
 * This module provides algorithms operating on matrix types such as
 * Fixed and Dynamic.
 *
 * Currently supported operations include:
 *
 * - Matrix multiplication.
 * - Fixed-to-fixed multiplication.
 * - Dynamic-to-dynamic multiplication.
 * - Fixed-to-dynamic multiplication.
 * - Dynamic-to-fixed multiplication.
 *
 * @section FoundationMatrixOperationsOverview Overview
 *
 * Matrix multiplication combines two matrices:
 *
 * A(M×N) · B(N×P) = C(M×P)
 *
 * The number of columns in the left matrix must match the number of
 * rows in the right matrix.
 *
 * @section FoundationMatrixOperationsFixed Fixed Matrices
 *
 * Fixed-size multiplication is resolved entirely at compile time.
 *
 * @code
 * Foundation::Math::Matrix::Fixed<float, 2, 3> A;
 * Foundation::Math::Matrix::Fixed<float, 3, 2> B;
 * Foundation::Math::Matrix::Fixed<float, 2, 2> C;
 *
 * Foundation::Math::Matrix::Multiply(A, B, C);
 * @endcode
 *
 * Matrix dimensions are validated by the compiler through template
 * parameters.
 *
 * @section FoundationMatrixOperationsDynamic Dynamic Matrices
 *
 * Dynamic matrices are checked at runtime.
 *
 * @code
 * Foundation::Math::Matrix::Dynamic<float> A;
 * Foundation::Math::Matrix::Dynamic<float> B;
 * Foundation::Math::Matrix::Dynamic<float> C;
 *
 * if (Foundation::Math::Matrix::Multiply(A, B, C)) {
 *     // Success
 * }
 * @endcode
 *
 * The result matrix is automatically resized when required. Compatible
 * attached storage is reused, so an embedded application can provide the
 * output buffer without allocating from the heap.
 *
 * `Multiply()` returns false without modifying the output when an input is
 * invalid, dimensions are incompatible, allocation fails, or output storage
 * overlaps either input. Pass a distinct result matrix; this includes matrices
 * attached to different regions of the same overlapping buffer.
 *
 * @section FoundationMatrixOperationsMixed Mixed Matrices
 *
 * Mixed multiplication between fixed and dynamic matrices is also
 * supported.
 *
 * Fixed × Dynamic:
 *
 * @code
 * Foundation::Math::Matrix::Fixed<float, 3, 3> A;
 * Foundation::Math::Matrix::Dynamic<float> B;
 * Foundation::Math::Matrix::Dynamic<float> C;
 *
 * Foundation::Math::Matrix::Multiply(A, B, C);
 * @endcode
 *
 * Dynamic × Fixed:
 *
 * @code
 * Foundation::Math::Matrix::Dynamic<float> A;
 * Foundation::Math::Matrix::Fixed<float, 3, 3> B;
 * Foundation::Math::Matrix::Dynamic<float> C;
 *
 * Foundation::Math::Matrix::Multiply(A, B, C);
 * @endcode
 *
 * @section FoundationMatrixOperationsComplexity Computational Complexity
 *
 * Matrix multiplication is implemented using the classical
 * triple-nested-loop algorithm:
 *
 * @code
 * for(i)
 *     for(j)
 *         for(k)
 * @endcode
 *
 * Time complexity:
 *
 * O(MNP)
 *
 * Memory complexity:
 *
 * O(1)
 *
 * excluding storage required for the result matrix.
 *
 * @section FoundationMatrixOperationsApplications Applications
 *
 * Typical applications include:
 *
 * - Coordinate transformations.
 * - Robotics kinematics.
 * - State-space control systems.
 * - DSP processing.
 * - Machine learning.
 * - Numerical linear algebra.
 *
 * @see Foundation::Math::Matrix::Fixed
 * @see Foundation::Math::Matrix::Dynamic
 */
